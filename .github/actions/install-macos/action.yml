name: Install homebrew packages on macOS

runs:
  using: composite
  steps:
    - name: Install homebrew packages
      run: |
        brew install \
          gdal \
          geos
      shell: bash
